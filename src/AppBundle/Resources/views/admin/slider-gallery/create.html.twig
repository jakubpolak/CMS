{% extends '@App/admin/layout.html.twig' %}

{% block heading %}{{ 'Banner'|trans({}, 'admin') }}{% endblock %}

{% block breadcrumbs %}&raquo; <a href="{{ path('admin_sliderGallery_index') }}">{{ 'Banner'|trans({}, 'admin') }}</a> &raquo; {{ 'Pridať obrázok'|trans({}, 'admin') }}{% endblock %}

{% block content %}
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">{{ 'Pridať obrázok'|trans({}, 'admin') }}</div>
                <div class="panel-body">
                    <form role="form" method="{{ form.vars.method }}" action="{{ form.vars.action }}" enctype="multipart/form-data">
                        {{ form_widget(form) }}
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}