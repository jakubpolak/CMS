{% if isSlugsEnabled() == true %}
    {% if languagesCount > slugs|length %}
        <div class="row app-margin-bottom-15">
            <div class="col-lg-12">
                <div class="btn-group pull-right" role="group">
                    <a href="{{ path('admin_slug_create', {'entityName': entityName, 'entityId': entityId}) }}" type="button" class="btn btn-primary btn-sm">{{ 'Nový slug'|trans({}, 'admin') }}</a>
                </div>
            </div>
        </div>
    {% endif %}
{% endif %}
